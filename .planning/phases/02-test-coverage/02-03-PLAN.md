---
phase: 02-test-coverage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/architecture/quantum_dots/test_macro_persistence.py
autonomous: true
requirements:
  - TEST-06
must_haves:
  truths:
    - "A save/load round-trip test for QuantumDot, QuantumDotPair, and SensorDot macro instances completes without error"
    - "Restored macros are functionally equivalent (macro keys present; expected types)"
  artifacts:
    - path: "tests/architecture/quantum_dots/test_macro_persistence.py"
      provides: "test_macro_instances_survive_save_load_roundtrip"
      contains: "def test_macro_instances_survive_save_load_roundtrip"
  key_links:
    - from: "test"
      to: "qd_machine, reset_catalog, tmp_path"
      via: "pytest fixtures"
      pattern: "qd_machine.*reset_catalog.*tmp_path"
    - from: "wire_machine_macros"
      to: "machine.save / BaseQuamQD.load"
      via: "round-trip flow"
      pattern: "wire_machine_macros|\.save|BaseQuamQD\.load"
---

<objective>
Create a new test file `tests/architecture/quantum_dots/test_macro_persistence.py` with a save/load round-trip test that wires qd_machine, saves to a temp path, loads via BaseQuamQD.load(), and asserts that quantum_dots, quantum_dot_pairs, and sensor_dots retain their macro keys (initialize, measure, empty where applicable; measure-only for SensorDot).

Purpose: TEST-06 requires regression protection for macro persistence. BaseQuamQD.load() calls wire_machine_macros(instance) after deserialization, so macros are re-wired from the registry. The test verifies the round-trip preserves structure. "Functionally equivalent" per CONTEXT: macro keys present and (optionally) types correct.

Output: New test file; pytest test_macro_persistence passes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-test-coverage/02-CONTEXT.md
@.planning/phases/02-test-coverage/02-RESEARCH.md
@.planning/phases/02-test-coverage/02-VALIDATION.md
</context>

<interfaces>
<!-- Key patterns from RESEARCH and test_stage_workflow_persistence. -->

From tests/architecture/quantum_dots/conftest.py:
- qd_machine: LossDiVincenzoQuam with quantum_dots, quantum_dot_pairs, sensor_dots
- Fixture available to tests under tests/architecture/quantum_dots/

From tests/conftest.py:
- reset_catalog: Resets _REGISTERED and _COMPONENT_MACRO_FACTORIES
- Any test calling wire_machine_macros must use reset_catalog

From test_stage_workflow_persistence.py:
```python
machine_stage1.save(stage1_dir)
machine_loaded = BaseQuamQD.load(stage1_dir)
```

From quam_builder/architecture/quantum_dots/qpu/base_quam_qd.py:
- BaseQuamQD.load() calls wire_machine_macros(instance) after deserialize
- Loaded instance has voltage_sequences recreated and macros re-wired

From RESEARCH (save/load test example):
- Use tmp_path (pytest built-in) for temp directory
- wire_machine_macros(qd_machine)
- qd_machine.save(tmp_path)
- loaded = BaseQuamQD.load(tmp_path)
- Assert macro keys on quantum_dots, quantum_dot_pairs, sensor_dots
- SensorDot: measure only; no initialize, no empty
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create save/load round-trip test for macro persistence</name>
  <files>tests/architecture/quantum_dots/test_macro_persistence.py</files>
  <action>
    Create a new file `tests/architecture/quantum_dots/test_macro_persistence.py` with:

    1. Module docstring: "Tests for macro persistence across save/load round-trips."
    2. Imports: pytest, wire_machine_macros from macro_engine, BaseQuamQD from qpu
    3. Test function `test_macro_instances_survive_save_load_roundtrip(qd_machine, reset_catalog, tmp_path)`:
       - Call wire_machine_macros(qd_machine)
       - Call qd_machine.save(tmp_path)
       - Call loaded = BaseQuamQD.load(tmp_path)
       - For each quantum_dot in loaded.quantum_dots.values(): assert "initialize", "measure", "empty" in qd.macros
       - For each quantum_dot_pair in loaded.quantum_dot_pairs.values(): assert "initialize", "measure", "empty" in pair.macros
       - For each sensor_dot in loaded.sensor_dots.values(): assert "measure" in sd.macros; assert "initialize" not in sd.macros; assert "empty" not in sd.macros

    Use tmp_path (pytest built-in) — no need for tempfile.mkdtemp or shutil.rmtree; pytest cleans up.

    Ensure reset_catalog is requested so wire_machine_macros sees clean registration state. The qd_machine fixture is from tests/architecture/quantum_dots/conftest.py — tests in the same directory or subdirs get it.
  </action>
  <verify>
    <automated>pytest tests/architecture/quantum_dots/test_macro_persistence.py -v --tb=short</automated>
  </verify>
  <done>Test passes; save/load round-trip preserves macro keys for all three component types.</done>
</task>

</tasks>

<verification>
After completion:
1. `pytest tests/architecture/quantum_dots/test_macro_persistence.py -v` — passes
2. `make test` — full suite passes
</verification>

<success_criteria>
- test_macro_instances_survive_save_load_roundtrip exists and passes
- QuantumDot: initialize, measure, empty present after load
- QuantumDotPair: initialize, measure, empty present after load
- SensorDot: measure present; initialize and empty absent after load
</success_criteria>

<output>
After completion, create `.planning/phases/02-test-coverage/02-03-SUMMARY.md`
</output>
