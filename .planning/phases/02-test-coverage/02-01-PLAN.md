---
phase: 02-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
must_haves:
  truths:
    - "Running `make test` produces passing assertions that QuantumDot.macros contains initialize, measure, and empty after wiring"
    - "Running `make test` produces passing assertions that QuantumDotPair.macros contains initialize, measure, and empty after wiring"
    - "Running `make test` produces passing assertions that SensorDot.macros contains measure but not initialize or empty after wiring"
    - "REQUIREMENTS.md marks TEST-01, TEST-02, TEST-03 as complete"
  artifacts:
    - path: "tests/architecture/quantum_dots/components/test_quantum_dot.py"
      provides: "TestQuantumDotCatalog"
      contains: "class TestQuantumDotCatalog"
    - path: "tests/architecture/quantum_dots/components/test_quantum_dot_pair.py"
      provides: "TestQuantumDotPairCatalog"
      contains: "class TestQuantumDotPairCatalog"
    - path: "tests/architecture/quantum_dots/components/test_sensor_dot.py"
      provides: "TestSensorDotCatalog"
      contains: "class TestSensorDotCatalog"
  key_links:
    - from: "TestQuantumDotCatalog"
      to: "wire_machine_macros + reset_catalog"
      via: "pytest fixtures"
      pattern: "qd_machine, reset_catalog"
    - from: "TestQuantumDotPairCatalog"
      to: "wire_machine_macros + reset_catalog"
      via: "pytest fixtures"
      pattern: "qd_machine, reset_catalog"
    - from: "TestSensorDotCatalog"
      to: "wire_machine_macros + reset_catalog"
      via: "pytest fixtures"
      pattern: "qd_machine, reset_catalog"
---

<objective>
Verify that the existing catalog registration tests (TestQuantumDotCatalog, TestQuantumDotPairCatalog, TestSensorDotCatalog) added in Phase 1 Plan 02 fully satisfy TEST-01, TEST-02, and TEST-03. Run the tests to confirm they pass, then update REQUIREMENTS.md to mark these requirements complete.

Purpose: Phase 1 Plan 02 already added the three test classes. Research confirms they assert the correct macro presence for all three component types. This plan validates that conclusion and updates traceability.

Output: REQUIREMENTS.md updated with TEST-01, TEST-02, TEST-03 marked complete; all catalog tests passing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-test-coverage/02-CONTEXT.md
@.planning/phases/02-test-coverage/02-RESEARCH.md
@.planning/phases/02-test-coverage/02-VALIDATION.md
</context>

<interfaces>
<!-- Key contracts from Phase 1 Plan 02. No code changes in this plan. -->

From tests/architecture/quantum_dots/components/test_quantum_dot.py:
- TestQuantumDotCatalog: test_has_initialize_macro, test_has_measure_macro, test_has_empty_macro
- Fixtures: qd_machine, reset_catalog

From tests/architecture/quantum_dots/components/test_quantum_dot_pair.py:
- TestQuantumDotPairCatalog: test_has_initialize_macro, test_has_measure_macro, test_has_empty_macro
- Fixtures: qd_machine, reset_catalog

From tests/architecture/quantum_dots/components/test_sensor_dot.py:
- TestSensorDotCatalog: test_has_measure_macro, test_no_initialize_macro, test_no_empty_macro
- Fixtures: qd_machine, reset_catalog

From .planning/REQUIREMENTS.md:
- TEST-01: QuantumDot default macros asserted
- TEST-02: QuantumDotPair default macros asserted
- TEST-03: SensorDot measure-only asserted; initialize/empty absent
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Verify catalog tests pass and mark requirements complete</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    1. Run the catalog tests to confirm they pass:
       `pytest tests/architecture/quantum_dots/components/ -k Catalog -v`
    2. If all tests pass, update .planning/REQUIREMENTS.md: change the checkbox for TEST-01, TEST-02, and TEST-03 from `[ ]` to `[x]`.

    The checkbox lines are under "### Tests" and look like:
    - `- [ ] **TEST-01**:` → `- [x] **TEST-01**:`
    - `- [ ] **TEST-02**:` → `- [x] **TEST-02**:`
    - `- [ ] **TEST-03**:` → `- [x] **TEST-03**:`

    If any catalog test fails, do NOT update REQUIREMENTS.md; instead diagnose and fix the failure first, then re-run and mark complete.
  </action>
  <verify>
    <automated>pytest tests/architecture/quantum_dots/components/ -k Catalog -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>All 9 catalog tests pass; REQUIREMENTS.md marks TEST-01, TEST-02, TEST-03 complete.</done>
</task>

</tasks>

<verification>
After completion:
1. `pytest tests/architecture/quantum_dots/components/ -k Catalog -v` — all 9 tests pass
2. `make test` — full suite passes
</verification>

<success_criteria>
- TestQuantumDotCatalog (3 tests), TestQuantumDotPairCatalog (3 tests), TestSensorDotCatalog (3 tests) all pass
- REQUIREMENTS.md has `[x]` for TEST-01, TEST-02, TEST-03
- Full test suite remains green
</success_criteria>

<output>
After completion, create `.planning/phases/02-test-coverage/02-01-SUMMARY.md`
</output>
