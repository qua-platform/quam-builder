---
phase: 04-customer-documentation
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - quam_builder/architecture/quantum_dots/examples/external_macro_package_example.py
  - quam_builder/architecture/quantum_dots/examples/external_macro_demo/__init__.py
  - quam_builder/architecture/quantum_dots/examples/external_macro_demo/catalog.py
autonomous: true
requirements:
  - DOCS-02

must_haves:
  truths:
    - "Standalone Python script runs to completion (exit 0) without QM hardware"
    - "Script demonstrates external macro package workflow: import build_macro_overrides, pass to wire_machine_macros"
    - "Custom macro class is self-contained, importable, and uses @quam_dataclass"
    - "Script references QuantumDot, QuantumDotPair, SensorDot and produces no runtime errors"
  artifacts:
    - path: quam_builder/architecture/quantum_dots/examples/external_macro_package_example.py
      provides: "Main script entrypoint"
      exports: "main()"
    - path: quam_builder/architecture/quantum_dots/examples/external_macro_demo/catalog.py
      provides: "build_macro_overrides and custom macro classes"
      contains: "@quam_dataclass"
  key_links:
    - from: quam_builder/architecture/quantum_dots/examples/external_macro_package_example.py
      to: external_macro_demo.catalog
      via: "from ...external_macro_demo.catalog import build_macro_overrides"
    - from: external_macro_package_example.py
      to: wire_machine_macros
      via: "wire_machine_macros(machine, macro_overrides=build_macro_overrides(), strict=True)"
---

<objective>
Create a standalone Python script and accompanying external macro demo package demonstrating the external macro package workflow. The script imports `build_macro_overrides` from a separate module, builds a tutorial machine (QuantumDot, QuantumDotPair, SensorDot), wires macros with overrides, and exits successfully.

Purpose: Customers can copy the pattern for lab-owned macro packages that survive upstream pulls.
Output: external_macro_package_example.py, external_macro_demo/catalog.py with build_macro_overrides and @quam_dataclass macro class.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-customer-documentation/04-RESEARCH.md
@.planning/phases/04-customer-documentation/04-VALIDATION.md
@quam_builder/architecture/quantum_dots/operations/README.md
@quam_builder/architecture/quantum_dots/examples/default_macro_overrides_example.py
@quam_builder/architecture/quantum_dots/examples/default_macro_defaults_example.py
@quam_builder/architecture/quantum_dots/operations/default_macros/state_macros.py
</context>

<interfaces>
From operations/README.md — external package pattern:

```python
def build_macro_overrides() -> dict:
    return {
        "component_types": {
            "QuantumDot": {"macros": {"initialize": {"factory": LabInitMacro, "params": {...}}}},
            ...
        }
    }
```

In experiment:
```python
from my_lab_qd_macros.catalog import build_macro_overrides
wire_machine_macros(machine, macro_overrides=build_macro_overrides(), strict=True)
```

All custom macro classes must use @quam_dataclass (from quam.core).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create external macro demo subpackage with build_macro_overrides</name>
  <files>quam_builder/architecture/quantum_dots/examples/external_macro_demo/__init__.py, quam_builder/architecture/quantum_dots/examples/external_macro_demo/catalog.py</files>
  <action>
Create the `external_macro_demo` package alongside existing examples:

1. `quam_builder/architecture/quantum_dots/examples/external_macro_demo/__init__.py`: Export `build_macro_overrides` and optionally the custom macro class. Minimal content.

2. `quam_builder/architecture/quantum_dots/examples/external_macro_demo/catalog.py`:
   - Define a custom macro class (e.g. `LabInitializeMacro`) that subclasses `InitializeStateMacro` from default_macros/state_macros.py. Use `@quam_dataclass` decorator. Add one extra serializable field (e.g. `lab_ramp_duration: int = 64`) to show parametrization.
   - Define `build_macro_overrides() -> dict` returning:
     ```python
     {
         "component_types": {
             "QuantumDot": {"macros": {"initialize": {"factory": LabInitializeMacro, "params": {"lab_ramp_duration": 80}}}},
             # Optionally add QuantumDotPair, SensorDot overrides if they have customizable macros
         }
     }
     ```
   - The function must be importable and the dict must be valid for `wire_machine_macros(..., macro_overrides=..., strict=True)`.

Follow the pattern from operations/README.md "Importing a Full Macro Catalog From Another Package". Use short class names to keep the example readable. All macro classes in this package must use `@quam_dataclass`.
</action>
  <verify>
    <automated>python -c "
from quam_builder.architecture.quantum_dots.examples.external_macro_demo.catalog import build_macro_overrides
o = build_macro_overrides()
assert 'component_types' in o
assert 'QuantumDot' in o['component_types']
print('ok')
"</automated>
  </verify>
  <done>external_macro_demo package exists; build_macro_overrides returns valid dict; LabInitializeMacro uses @quam_dataclass</done>
</task>

<task type="auto">
  <name>Task 2: Create external_macro_package_example.py script</name>
  <files>quam_builder/architecture/quantum_dots/examples/external_macro_package_example.py</files>
  <action>
Create `quam_builder/architecture/quantum_dots/examples/external_macro_package_example.py` that:

1. Imports `build_tutorial_machine` from `tutorial_machine` (from Plan 01)
2. Imports `build_macro_overrides` from `quam_builder.architecture.quantum_dots.examples.external_macro_demo.catalog`
3. Imports `wire_machine_macros`
4. In `main()`:
   - Build machine via `build_tutorial_machine()`
   - Add voltage step points for state macros (reuse helper from tutorial_machine or inline minimal logic)
   - Call `wire_machine_macros(machine, macro_overrides=build_macro_overrides(), strict=True)`
   - Optionally build a small `qua.program()` that calls `initialize()`/`measure()` on a component (no qm.run)
   - Print success message (e.g. "Built QUA program successfully with external macro overrides.")
   - Do NOT call qm.open(), qm.run(), or machine.connect()

5. Add `if __name__ == "__main__": main()` guard

Docstring: Explain that this script demonstrates the external macro package workflow — custom macros in a separate package, imported and passed to wire_machine_macros. Runs without QM hardware. Reference QuantumDot, QuantumDotPair, SensorDot as the component types used.

Style: Follow default_macro_overrides_example.py and default_macro_defaults_example.py for structure and docstrings.
</action>
  <verify>
    <automated>python quam_builder/architecture/quantum_dots/examples/external_macro_package_example.py 2>&1 | tail -5</automated>
  </verify>
  <done>Script exits 0; uses build_macro_overrides; no hardware calls; references correct component types</done>
</task>

</tasks>

<verification>
- Run `make test` after tasks to confirm no regressions
- Phase gate: `python quam_builder/architecture/quantum_dots/examples/external_macro_package_example.py` exits 0
</verification>

<success_criteria>
1. DOCS-02: Standalone Python script exists demonstrating external macro package workflow
2. Script uses self-contained, importable macro class from external_macro_demo package
3. Script runs without QM hardware and references QuantumDot, QuantumDotPair, SensorDot
</success_criteria>

<output>
After completion, create `.planning/phases/04-customer-documentation/04-02-SUMMARY.md`
</output>
