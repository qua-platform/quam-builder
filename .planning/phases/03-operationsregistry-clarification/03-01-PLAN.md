---
phase: 03-operationsregistry-clarification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quam_builder/architecture/quantum_dots/operations/default_operations.py
  - quam_builder/architecture/quantum_dots/operations/README.md
autonomous: true
requirements:
  - OPS-01
  - OPS-02

must_haves:
  truths:
    - "Developer reading default_operations.py sees clear explanation of when to use registry vs direct dispatch"
    - "Developer reading operations/README.md sees comparison table with all three invocation paths"
    - "Table has 'when to use' and 'applicable component types' for each row"
  artifacts:
    - path: quam_builder/architecture/quantum_dots/operations/default_operations.py
      provides: "OperationsRegistry module docstring"
      contains: "operations_registry.x180(q) vs q.x180()"
    - path: quam_builder/architecture/quantum_dots/operations/README.md
      provides: "Three-way invocation comparison table"
      contains: "operations_registry.x180(q)|q.x180()|q.macros"
  key_links:
    - from: quam_builder/architecture/quantum_dots/operations/default_operations.py
      to: "OperationsRegistry role"
      via: "module docstring at top of file"
    - from: quam_builder/architecture/quantum_dots/operations/README.md
      to: "invocation paths"
      via: "table section with three rows"
---

<objective>
Add module docstring (OPS-01) and README comparison table (OPS-02) so developers understand when to use `operations_registry.x180(q)` vs `q.x180()` vs `q.macros["x180"].apply()`.

Purpose: Eliminate ambiguity for library users and contributors; no new production logic.
Output: Updated default_operations.py docstring, new README section with three-row table.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-operationsregistry-clarification/03-CONTEXT.md
@.planning/phases/03-operationsregistry-clarification/03-RESEARCH.md
@quam_builder/architecture/quantum_dots/operations/default_operations.py
@quam_builder/architecture/quantum_dots/operations/README.md
</context>

<interfaces>
<!-- Existing content the executor needs. -->

From default_operations.py (current module docstring):
```python
"""Gate-level operations for quantum-dot components using QuAM OperationsRegistry.

Each registered function is intentionally empty because ``OperationsRegistry``
uses the function signature/name as operation metadata and dispatches calls to
``component.macros[operation_name]`` at runtime.
"""
```

From 03-CONTEXT.md (locked decisions):
- Docstring: compare ONLY operations_registry.x180(q) vs q.x180(); full three-way in README
- Prose with inline backticks — no code blocks in module docstring
- 3–5 sentences; fresh reader; both audiences (users first, contributors)
- "OperationsRegistry is NOT required for most users — q.x180() is the natural direct call"
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Expand OperationsRegistry module docstring (OPS-01)</name>
  <files>quam_builder/architecture/quantum_dots/operations/default_operations.py</files>
  <action>
Replace the existing 5-line module docstring with an expanded version (3–5 sentences) that:

1. Briefly states what OperationsRegistry is (assume fresh reader; do not assume familiarity with QuAM dispatch internals)
2. Compares ONLY `operations_registry.x180(q)` vs `q.x180()` — use prose with inline backticks, no code blocks
3. Clarifies when to use each: use `operations_registry.x180(q)` when writing generic algorithms that work across component types; use `q.x180()` for component-specific code where the component type is known
4. States explicitly that OperationsRegistry is NOT required for most users — `q.x180()` is the natural direct call; the registry is a convenience for protocol-style code
5. Preserve the existing explanation of function signatures as metadata and dispatch to component.macros

Style: Prose only, inline backticks for identifiers. Target 3–5 sentences. Write for library users first, then contributors.
</action>
  <verify>
    <automated>make test</automated>
  </verify>
  <done>Module docstring is 3–5 sentences; compares registry vs direct dispatch; prose-only with inline backticks; states q.x180() is natural choice for most users</done>
</task>

<task type="auto">
  <name>Task 2: Add invocation comparison table to README (OPS-02)</name>
  <files>quam_builder/architecture/quantum_dots/operations/README.md</files>
  <action>
Add a new top-level section to operations/README.md with a three-row comparison table. Placement: after "Architecture Overview" (or after "Default Macro Logic by Component Type"), before "When and Where Macros Are Wired" — per 03-RESEARCH placement recommendation.

Table requirements:
- Three rows: `operations_registry.x180(q)`, `q.x180()`, `q.macros["x180"].apply()`
- Required columns: "Invocation" (or similar), "When to use", "Applicable component types"
- Optional: add "dispatch path" or "example" column if it improves clarity (Claude's discretion)

"When to use" guidance from 03-RESEARCH:
- operations_registry.x180(q): Generic algorithms, type-safe protocol code, IDE completion
- q.x180(): Component-specific code; natural direct call; includes sticky-voltage tracking
- q.macros["x180"].apply(): Lowest-level access; bypasses compiled dispatch and sticky-voltage tracking; use when you need the macro object itself (introspection, custom dispatch) or when tracking is irrelevant

"Applicable component types" from 03-RESEARCH:
- Registry and q.x180(): LDQubit (1Q gates); LDQubitPair (2Q gates); QuantumDot, QuantumDotPair, SensorDot (state macros)
- Direct .apply(): Any component with macros dict

Use ## heading for the new section. Follow existing README markdown style (short code blocks if helpful; prose-only also acceptable).
</action>
  <verify>
    <automated>make test</automated>
  </verify>
  <done>README contains dedicated section with three-row table; each row has "when to use" and "applicable component types"; table is discoverable near top of README</done>
</task>

</tasks>

<verification>
- Run `make test` after each task to confirm no regressions (documentation-only; no new tests required)
- Phase gate: Human review of docstring and table for clarity before /gsd:verify-work
</verification>

<success_criteria>
1. OPS-01: Module docstring in default_operations.py is 3–5 sentences, prose-only, compares operations_registry.x180(q) vs q.x180(), and states q.x180() is natural for most users
2. OPS-02: operations/README.md contains a three-row table with "when to use" and "applicable component types" for each invocation path
</success_criteria>

<output>
After completion, create `.planning/phases/03-operationsregistry-clarification/03-01-SUMMARY.md`
</output>
